# Copy to /etc/default/inbx and edit.

# Required for read auth
INBX_USER=inbx
INBX_PASS=change-this-password

# Optional fixed token for POST /inbx.
# If unset, the app auto-generates a token on first start and stores it in
# the storage directory. You can rotate/unset it from /inbx/view.
# INBX_POST_TOKEN=replace-with-token

# Plain-text storage directory. Leave unset with the provided systemd unit:
# it sets INBX_STORAGE_PATH to the StateDirectory path automatically.
# INBX_STORAGE_PATH=/var/lib/inbx

# Keep only newest N entries
INBX_MAX_ENTRIES=100

# Optional app secret (used by Mojolicious internals)
INBX_SECRET=change-this-secret

# Listener URL for hypnotoad.
# Default from the unit uses systemd socket activation:
# MOJO_LISTEN=http://127.0.0.1?fd=3
